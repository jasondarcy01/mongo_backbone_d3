var Multifamily = Backbone.Model.extend({
  urlRoot:  "/multifamily", // automatic `id` addition
  // `get` pulls from `attributes`
  // `attributes` populated by server response

});

var MultifamilyView = Backbone.View.extend({
  initialize: function() {

  }
  /* etc */
});